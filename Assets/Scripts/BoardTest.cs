using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BoardTest : MonoBehaviour
{
    void Start()
    {
        // Debug.Log("Now Rooks");
        // foreach(Magics m in MagicBitboards.RookMagics) {
        //     string s = "";
        //     foreach(ulong move in m.moves) s += " " +move;
        //     Debug.Log("The magic: " + m.magic + " & the number of items: " + m.moves.Length + " & the items in moves: " + s);
        // }
        // Debug.Log("Now Bishops");
        // foreach(Magics m in MagicBitboards.BishopMagics) {
        //     string s = "";
        //     foreach(ulong move in m.moves) s += " " +move;
        //     Debug.Log("The magic: " + m.magic + " & the number of items: " + m.moves.Length + " & the items in moves: " + s);
        // }
    }

    void Update()
    {
        
    }
}
